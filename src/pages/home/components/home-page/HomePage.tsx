import { CityList } from "@/widgets/city-list";
import { Search } from "@/widgets/search";

export const HomePage = () => {
  return (
    <div>
      <Search />
      <CityList />
    </div>
  );
};
